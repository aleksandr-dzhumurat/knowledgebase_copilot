# Core LangChain and RAG Dependencies
langchain==0.1.0
langchain-community==0.0.13

# Ollama Integration
ollama==0.1.6

# Vector Database
chromadb==0.4.22

# Document Loaders
pypdf==3.17.4
python-docx==1.1.0
docx2txt==0.8
unstructured==0.11.8
# docling  # For PDF OCR with VLM (used in src/ocr_graphite.py)
# python-magic-bin not compatible with Apple Silicon
# Install libmagic via homebrew instead: brew install libmagic
# Then install: pip install python-magic

# File Watching (for automation)
watchdog==3.0.0

# Embeddings and Text Processing
sentence-transformers==2.2.2

# HTTP Requests (for Ollama verification)
requests==2.31.0

# Utilities
python-dotenv==1.0.0  # For environment variables
pync==2.0.3  # macOS notifications (optional)

# Progress bars (optional but nice)
tqdm==4.66.1

# HTML Parsing
beautifulsoup4==4.12.3

# Token counting
tiktoken

markdown_pdf
pypandoc

google-genai
google-cloud-storage